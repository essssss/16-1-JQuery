// JQUERY
// First thing we do is the $ sign. 
// $ acts like "querySelectorAll"
// $ is an alias for the jQuery() function.

// We don't get a node List. we get a jQuery object!

// NOT the same as DOM elements

// We can use ".get" to get an element or array.

// ************************************************** //
// jQuery getter/setter methods.

// ".text()" allows us to get the inner text. But ALSO, we can change it , with one swoop

// With jQuery we can frequently updat multiple elements at once. Without looping.


